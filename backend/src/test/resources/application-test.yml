spring:
  datasource:
    url: jdbc:h2:mem:caselens;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none

caselens:
  session-secret: test-session-secret-that-is-at-least-thirty-two-bytes
  triage-queue-url: http://localhost/unused
